# Backend API base URL for Vite dev / production builds that call a separate host.
# Leave empty for same-origin (when frontend is served by FastAPI via deploy-local.bat).
VITE_API_URL=http://localhost:8100
